#include <stack>
#include <algorithm>

using namespace std;

class MinStack {
private:
    stack<int> main_st; // Primary stack for standard operations
    stack<int> min_st;  // Secondary stack to track dynamic minimums

public:
    MinStack() {
        // Constructor initialization
    }
    
    void push(int value) {
        main_st.push(value);
        
        // If min_st is empty, the current value is the minimum.
        // Otherwise, compare with the current top of min_st.
        if (min_st.empty()) {
            min_st.push(value);
        } else {
            min_st.push(min(value, min_st.top()));
        }
    }
    
    void pop() {
        main_st.pop();
        min_st.pop();
    }
    
    int top() {
        return main_st.top();
    }
    
    int getMin() {
        return min_st.top();
    }
};

/**
 * Your MinStack object will be instantiated and called as such:
 * MinStack* obj = new MinStack();
 * obj->push(value);
 * obj->pop();
 * int param_3 = obj->top();
 * int param_4 = obj->getMin();
 */
